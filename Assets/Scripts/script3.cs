using JetBrains.Annotations;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class script3 : MonoBehaviour
{
    // Start is called before the first frame update
    public string ed = "Humankind cannot gain anything without first giving something in return.";
    public string al = "To obtain, something of equal value must be lost.";
    public string winry = "That is Alchemy's first law of Equivalent Exchange.";
    public string resembool = "In those days, we really believed that to be the world's one and only truth.";
    void Start()
    {
        Debug.Log(ed + al + winry + resembool);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
