using JetBrains.Annotations;
using System.Collections;
using System.Collections.Generic;
using Unity.PlasticSCM.Editor.WebApi;
using UnityEngine;

public class script1 : MonoBehaviour
{
    // Start is called before the first frame update
    public char butter = 'b';
    public char twobutter = 'u';
    public char threebutter = 't';
    public char fourbutter = 'e';
    public char fivebutter = 'r';
    
    void Start()
    {
        Debug.Log(butter.ToString() + twobutter.ToString() + threebutter.ToString() + threebutter.ToString() + fourbutter.ToString() + fivebutter.ToString());


    }

    // Update is called once per frame
    void Update()
    {
    
    }
}
